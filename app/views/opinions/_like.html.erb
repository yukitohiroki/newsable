<% if @like.new_record? %>
  <%= button_to "#{@opinion.likes.count} いいね", [@clip, @opinion, @like], class: 'btn btn-danger', remote: true %>
<% else %>
  <%= button_to "#{@opinion.likes.count} いいねを取り消す", [@clip, @opinion, @like], class: 'btn btn-default', method: :delete, remote: true %>
<% end %>