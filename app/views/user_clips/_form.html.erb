<%= form_with(model: user_clip, local: true) do |form| %> 
 <% if clip = user_clip.clip %>
  <% if clip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clip.errors.count, "error") %>prohibited this user_clip from being saved:</h2>
      <ul>
        <% clip.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% end %>
  <div class="form-group">
    <%= text_field_tag :url,'', class: "form-control", id: "exampleFormControlTextarea1", rows: "3" , placeholder: "ニュース記事のURLを入力して下さい"%>
  </div>
  <div class="actions">
    <%= form.submit "Create Clip",  class: "btn btn-primary",  style: "width:150px;"%>
  </div>
<% end %>