<%= form_with(model: categoy, local: true) do |form| %>
  <% if categoy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categoy.errors.count, "error") %> prohibited this categoy from being saved:</h2>

      <ul>
      <% categoy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
